{
  "name": "js13k-littlejs-starter",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "sync": "for dir in templates/typescript/src templates/vanilla/src templates/modular/src; do cp -r templates/shared/. $dir; done",
    "zip": "vite build && node scripts/build.js",
    "deploy": "node scripts/checkGit.js && npm run zip && gh-pages -d dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "chalk": "^6.0.0",
    "gh-pages": "^6.3.0",
    "google-closure-compiler": "^20260726.0.0",
    "roadroller": "^2.1.0",
    "terser": "^5.49.0",
    "uglify-js": "~3.17.4",
    "vite": "^8.2.1"
  }
}
